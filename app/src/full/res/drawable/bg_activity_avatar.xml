<?xml version="1.0" encoding="utf-8"?>
<!-- circular avatar background used by the per-app activity list;
     tinted at bind time with the theme's colorPrimary (low alpha) -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" />
</shape>
