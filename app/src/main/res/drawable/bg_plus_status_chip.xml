<?xml version="1.0" encoding="utf-8"?>
<!-- Unified status/latency chip used across Rethink Plus screens.
     Ripple-wrapped so clickable hosts (e.g. the per-server "Apps · N"
     chip in the VPN server list) get press feedback; the ripple is
     inert on non-clickable views. -->
<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?attr/colorControlHighlight">

    <item>
        <shape android:shape="rectangle">
            <corners android:radius="12dp" />
            <solid android:color="?attr/vpnHeaderGradientStart" />
            <stroke
                android:width="1dp"
                android:color="?attr/serverHeaderStrokeColor" />
        </shape>
    </item>

</ripple>
